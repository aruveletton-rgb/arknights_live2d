# Server Deployment（未包含实现）

本次 `client.zip` 没有服务器部署配置。计划书中的目标拓扑为：VM-1 Gateway + Orchestrator、VM-2 TTS Primary、VM-3 ASR Gateway、VM-4 Fallback + Backup + Healthcheck。

本文件仅用于保持统一文档结构；正式部署说明应由线程 B/C 补充，不能把本文件视为可执行部署指南。
